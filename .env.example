# Database (SQLite, relative path for dev; absolute /app/data/prod.db in prod)
DATABASE_URL="file:./dev.db"

# AI enrichment
AI_ENRICHMENT_ENABLED=false
AI_API_KEY=""
AI_MODEL=""

# Public URL (prod only)
# NEXT_PUBLIC_SERVER_URL="https://leads.builtbetter.cc"

# Password gate (prod only)
# GATE_PASSWORD=""
# GATE_COOKIE_NAME="bb_gate_leads"
# GATE_SECRET="<openssl rand -hex 32>"

# Trust Cloudflare for client IP (prod only — must be literal string "true")
# CF_TRUSTED="true"
